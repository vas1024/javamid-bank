apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-business-dashboard
  labels:
    grafana_dashboard: "true"
data:
  business-metrics.json: |
    {{ .Files.Get "business-metrics-dashboard.json" | indent 4 }}